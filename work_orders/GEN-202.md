[WORK_ORDER]
id: GEN-202
goal: `pipeline/analyzers_extension.py`를 legacy shim으로 교체하고 canonical 경로를 단일화한다.

context:
- 정책 기준: `docs/architecture/ADV_004_ANALYZERS_EXTENSION_POLICY_V1.md`.
- 현재 extension은 drift 상태이며 import 오류가 발생할 수 있다.
- canonical 구현은 `pipeline/analyzers.py`(및 shard)다.

scope_files:
- pipeline/analyzers_extension.py
- docs/architecture/ADV_004_ANALYZERS_EXTENSION_POLICY_V1.md

tasks:
1. `pipeline/analyzers_extension.py` 내부 구현을 제거한다.
2. legacy shim으로 교체한다:
   - `analyze_volume_anomalies`
   - `track_events_with_news`
   - `run_adaptive_portfolio`
   위 3개를 `pipeline.analyzers`에서 re-export.
3. 모듈 import 시 `DeprecationWarning`을 남기되 실행을 막지 않는다.
4. public 함수명/시그니처는 유지한다.

constraints:
- 구조 결정 금지 (설계 변경 금지)
- scope 외 파일 수정 금지
- canonical 함수 로직 수정 금지

validation:
- python3 -m py_compile pipeline/analyzers_extension.py pipeline/analyzers.py
- python3 -c "from pipeline.analyzers_extension import run_adaptive_portfolio; print('ok')"
- python3 -c "from pipeline.analyzers_extension import analyze_volume_anomalies; print('ok')"

deliverables:
- 변경 파일 목록
- shim 코드 요약
- validation 결과
[/WORK_ORDER]
