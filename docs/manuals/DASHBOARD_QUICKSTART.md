# EIMAS 대시보드 빠른 시작 가이드

> 3분 안에 EIMAS 실시간 대시보드 실행하기

## 사전 요구사항

- Node.js 18+ 설치됨
- Python 3.10+ 설치됨
- EIMAS 프로젝트가 `/home/tj/projects/autoai/eimas`에 있음

## Step 1: 의존성 설치 (최초 1회)

```bash
cd /home/tj/projects/autoai/eimas/frontend
npm install
```

## Step 2: 3개 터미널 열기

### 터미널 1: FastAPI 서버

```bash
cd /home/tj/projects/autoai/eimas
uvicorn api.main:app --reload --port 8000
```

출력:
```
INFO:     Uvicorn running on http://127.0.0.1:8000
```

### 터미널 2: EIMAS 분석 (최소 1회)

```bash
cd /home/tj/projects/autoai/eimas
python main.py --quick
```

출력:
```
📊 DATA: FRED RRP=$5B, Net Liq=$5799B, Market 24 tickers
📈 REGIME: Bull (Low Vol), Risk 11.5/100
🤖 DEBATE: FULL=BULLISH, REF=BULLISH (Agree ✓)
🎯 FINAL: BULLISH, Confidence 65%, Risk MEDIUM
```

### 터미널 3: 프론트엔드

```bash
cd /home/tj/projects/autoai/eimas/frontend
npm run dev
```

출력:
```
  ▲ Next.js 16.0.10
  - Local:   http://localhost:3000
```

## Step 3: 브라우저 접속

http://localhost:3000 열기

## 예상 화면

```
┌────────────────────────────────────────────────────────┐
│  EIMAS Real-Time Dashboard                    🟢 Live  │
├────────────────────────────────────────────────────────┤
│                                                         │
│  ┌──────────────────────────────────────────────────┐  │
│  │  Final Recommendation: BULLISH                   │  │
│  │  Confidence: 65% ████████░░                      │  │
│  │  Risk Level: MEDIUM  11.5                        │  │
│  └──────────────────────────────────────────────────┘  │
│                                                         │
│  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐         │
│  │ Market │ │   AI   │ │  Data  │ │Market  │         │
│  │ Regime │ │Consensus│ │Collect │ │Quality │         │
│  │        │ │        │ │        │ │        │         │
│  │  Bull  │ │ Agree  │ │  24    │ │  65.2  │         │
│  │Low Vol │ │   ✓    │ │tickers │ │  /100  │         │
│  └────────┘ └────────┘ └────────┘ └────────┘         │
└────────────────────────────────────────────────────────┘
```

## 자동 업데이트 확인

1. 터미널 2에서 `python main.py --quick` 다시 실행
2. 5초 이내 브라우저가 자동으로 업데이트됨
3. 타임스탬프 변경 확인

## 문제 해결

### "Failed to load EIMAS analysis data"

**원인**: FastAPI 서버가 실행되지 않음

**해결**:
```bash
# 터미널 1에서
uvicorn api.main:app --reload --port 8000
```

### "No integrated results found"

**원인**: outputs 디렉토리에 JSON 파일이 없음

**해결**:
```bash
# 터미널 2에서
python main.py --quick
```

### 포트 충돌

**원인**: 8000 또는 3000 포트가 이미 사용 중

**해결**:
```bash
# FastAPI 포트 변경
uvicorn api.main:app --reload --port 8001

# Next.js 포트 변경
npm run dev -- -p 3001
```

그리고 `frontend/lib/api.ts`에서:
```typescript
const API_BASE_URL = "http://localhost:8001" // 8000 → 8001
```

## 추가 명령어

### 프로덕션 빌드
```bash
cd frontend
npm run build
npm start
```

### 백그라운드 실행
```bash
# FastAPI 백그라운드 실행
uvicorn api.main:app --reload --port 8000 &

# 프론트엔드 실행
cd frontend
npm run dev
```

### 모두 종료
```bash
# Ctrl+C로 각 터미널 종료
# 또는
pkill -f "uvicorn api.main"
pkill -f "next dev"
```

## 다음 단계

- 폴링 간격 변경: `frontend/components/MetricsGrid.tsx`
- 색상 커스터마이징: Tailwind 클래스 수정
- 새 메트릭 추가: `MetricsGrid.tsx`에 Card 추가
- AI 리포트 추가: `python main.py --report` 실행 후 UI에 표시

## 참고 자료

- EIMAS 전체 문서: `/home/tj/projects/autoai/eimas/CLAUDE.md`
- 프론트엔드 상세: `/home/tj/projects/autoai/eimas/frontend/README.md`
- API 문서: http://localhost:8000/docs (FastAPI 실행 후)

---

**완료!** 이제 EIMAS 실시간 대시보드가 5초마다 자동으로 업데이트됩니다.
